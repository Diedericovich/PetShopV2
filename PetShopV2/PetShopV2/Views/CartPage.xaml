<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:PetShopV2.ViewModels"
             Title="{Binding Title}"
             x:Class="PetShopV2.Views.CartPage">
    <ContentPage.BindingContext>
        <local:CartViewModel/>
    </ContentPage.BindingContext>


    <ContentPage.Content>
        <RefreshView Command="{Binding LoadProductsCommand}" 
                 IsRefreshing="{Binding IsBusy, Mode=TwoWay}">
            <StackLayout BackgroundColor="Blue">
                <CollectionView x:Name="CartListView"
                ItemsSource="{Binding CartSingleton.ShoppingCart.ItemsInCart}"
                SelectionMode="Single">

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Grid   HeightRequest="100"  
                                Margin="5" 
                                BackgroundColor="Blue" >
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="125"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="60"/>
                                </Grid.ColumnDefinitions>

                                <Image Source="{Binding Product.Image }"
                               HeightRequest="100"
                               HorizontalOptions="Start" />

                                <Label Grid.Column="1" 
                                       VerticalOptions="Center" 
                                         Text="{Binding Product.Name}"
                                         LineBreakMode="NoWrap"
                                            Style="{DynamicResource ListItemTextStyle}"
                                         FontSize="16" />
                                <Label Grid.Column="2"
                                    Text="{Binding Product.Price}"
                                    LineBreakMode="NoWrap"
                                    Style="{DynamicResource ListItemTextStyle}"
                                    FontSize="16" />
                                <Label Text="{Binding Product.InStock}"
                                LineBreakMode="NoWrap"
                                Style="{DynamicResource ListItemDetailTextStyle}"
                                FontSize="13" />


                                <Grid.GestureRecognizers>
                                    <TapGestureRecognizer
                                NumberOfTapsRequired="1"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type local:CartViewModel}}, 
                                    Path=ProductTapped}"
                                CommandParameter="{Binding .}"/>
                                </Grid.GestureRecognizers>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </StackLayout>
        </RefreshView>




    </ContentPage.Content>
</ContentPage>